# 01 — 架构设计文档

---

## 1. 系统总体架构

PNNE 采用**分层架构 + 事件驱动**的混合模式，共分为 5 大层级：

```
┌─────────────────────────────────────────────────────────┐
│                     Application Layer                    │
│  main.py · Settings · Plugin Manager · Project Manager   │
├─────────────────────────────────────────────────────────┤
│                     UI Layer (PyQt6)                      │
│  MainWindow · GraphicsView · Panels · Widgets · Dialogs  │
├─────────────────────────────────────────────────────────┤
│                    Bridge Layer (Signals)                 │
│  GlobalSignalBus · UndoStack · Clipboard · EventFilter   │
├─────────────────────────────────────────────────────────┤
│                     Core Layer                           │
│  Node · Pin · Connection · NodeGraph · PathManager       │
│  NodeRegistry · NodeFactory · ExpressionEngine           │
├─────────────────────────────────────────────────────────┤
│                    Engine Layer                           │
│  GraphExecutor · TorchCompiler · TrainingEngine           │
│  InferenceEngine · VisualizationEngine                   │
└─────────────────────────────────────────────────────────┘
```

### 1.1 各层职责

| 层级 | 职责 | 可访问的下层 |
|------|------|------------|
| **Application** | 启动流程、全局配置、插件加载、项目管理 | 所有层 |
| **UI** | 用户交互、图形渲染、事件处理 | Bridge、Core |
| **Bridge** | 信号转发、命令调度、状态同步 | Core |
| **Core** | 数据模型、节点逻辑、表达式求值 | Engine |
| **Engine** | PyTorch 执行、训练调度、模型编译 | PyTorch/NumPy |

### 1.2 层间通信规则

1. **上层 → 下层**: 直接方法调用
2. **下层 → 上层**: Qt 信号 (Signal) 或回调函数 (Callback)
3. **同层**: 通过 GlobalSignalBus 广播事件
4. **禁止**: UI 层直接操作 Engine 层；Engine 层直接修改 UI

---

## 2. 核心数据模型类图

```
                          ┌──────────────┐
                          │  NodeGraph   │
                          │──────────────│
                          │ name         │
                          │ nodes: Dict  │
                          │ connections  │
                          │ paths: Dict  │
                          │ context: Dict│
                          ├──────────────┤
                          │ add_node()   │
                          │ remove_node()│
                          │ connect()    │
                          │ disconnect() │
                          │ execute()    │
                          │ topo_sort()  │
                          │ find_by_path()│
                          │ to_dict()    │
                          │ from_dict()  │
                          └──────┬───────┘
                                 │ 1:N
                    ┌────────────┼────────────┐
                    │                         │
             ┌──────▼──────┐          ┌───────▼───────┐
             │    Node     │          │  Connection   │
             │─────────────│          │───────────────│
             │ id          │          │ id            │
             │ node_name   │          │ source_node   │
             │ node_label  │          │ source_pin    │
             │ node_type   │          │ target_node   │
             │ parent      │          │ target_pin    │
             │ children    │          │ enabled       │
             │ pins: Dict  │          ├───────────────┤
             │ properties  │          │ to_dict()     │
             │ details     │          │ from_dict()   │
             │ module      │          └───────────────┘
             │ enabled     │
             │ visible     │
             │ position    │
             ├─────────────┤
             │ add_pin()   │
             │ forward()   │
             │ backward()  │
             │ get_path()  │
             │ to_dict()   │
             │ from_dict() │
             └──────┬──────┘
                    │ 1:N
             ┌──────▼──────┐
             │     Pin     │
             │─────────────│
             │ id          │
             │ name        │
             │ direction   │
             │ data_type   │
             │ default_val │
             │ connections │
             │ owner_node  │
             │ cached_data │
             ├─────────────┤
             │ connect()   │
             │ disconnect()│
             │ get_data()  │
             │ get_shape() │
             │ to_dict()   │
             │ from_dict() │
             └─────────────┘
```

---

## 3. 节点继承体系

```
                         Node (基类)
                           │
          ┌────────────────┼────────────────────┐
          │                │                    │
     NNLayerNode      DataNode           LogicNode
          │                │                    │
    ┌─────┼──────┐    ┌───┼────┐        ┌──────┼──────┐
    │     │      │    │   │    │        │      │      │
 Linear Conv2d ReLU Image Text OBJ  ForEach Switch  Merge
    │     │      │    │   │    │   Begin/End    │      │
 BatchNorm Pool Softmax CSV Dataset DL  Null  ...    ...
    │
 Dropout  RNN  LSTM  GRU  Transformer
    │
 LossNode  OptimizerNode
```

```
                         Node (基类)
                           │
          ┌────────────────┼────────────────┐
          │                │                │
     SubnetNode      PythonNode      ContextNode
          │                                  │
    ┌─────┼─────┐                  ┌────────┼────────┐
    │           │                  │        │        │
 SubnetInput SubnetOutput     ObjContext VisContext TrainContext
```

---

## 4. 路径系统架构

### 4.1 路径树结构

```
/ (root)
├── obj/                     ← ObjContextNode (模型空间)
│   └── nnmodel/             ← SubnetNode (主模型)
│       ├── conv1            ← Conv2dNode
│       ├── bn1              ← BatchNorm2dNode
│       ├── relu1            ← ReLUNode
│       ├── resblock/        ← SubnetNode (ResNet Block)
│       │   ├── _input_      ← SubnetInputNode (4个输入口)
│       │   ├── conv_a       ← Conv2dNode
│       │   ├── bn_a         ← BatchNorm2dNode
│       │   ├── relu_a       ← ReLUNode
│       │   ├── conv_b       ← Conv2dNode
│       │   ├── add          ← MergeNode (残差相加)
│       │   └── _output_     ← SubnetOutputNode
│       └── fc1              ← LinearNode
│
├── vis/                     ← VisContextNode (可视化空间)
│   └── monitor/             ← VisdomNode / TensorBoardNode
│       ├── loss_plot        ← 损失曲线
│       └── accuracy_plot    ← 准确率曲线
│
└── train/                   ← TrainContextNode (训练空间)
    ├── dataset/             ← DatasetNode
    ├── dataloader/          ← DataLoaderNode
    ├── foreach_begin/       ← ForEachBeginNode (epoch循环)
    ├── train_step/          ← 训练步骤子网
    └── foreach_end/         ← ForEachEndNode
```

### 4.2 PathManager 设计

```python
class PathManager:
    """
    管理全局节点路径树。
    路径格式: /context/parent/.../node_name
    
    核心方法:
    - resolve(path: str) -> Node          # 路径 → 节点
    - get_path(node: Node) -> str         # 节点 → 路径
    - move(node: Node, new_parent: str)   # 移动节点
    - rename(node: Node, new_name: str)   # 重命名
    - list_children(path: str) -> List    # 列出子节点
    - exists(path: str) -> bool           # 路径是否存在
    """
```

### 4.3 路径规则

| 规则 | 说明 |
|------|------|
| 路径分隔符 | `/` |
| 根路径 | `/obj/`, `/vis/`, `/train/` |
| 节点名唯一性 | 同一父节点下 node_name 唯一 |
| 自动命名 | 新节点自动获得 `type1`, `type2` ... 递增名 |
| 重命名验证 | 不允许包含 `/`, 空格, 特殊字符 |
| 动态图限制 | 只有 `/obj/` 路径下可创建 nn.Module 节点 |

---

## 5. 执行引擎架构

### 5.1 图执行流程

```
用户点击 "Run Graph"
        │
        ▼
┌─────────────────┐
│  GraphExecutor  │
│  .execute()     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌──────────────────┐
│  拓扑排序        │────▶│  检测循环依赖     │
│  topological    │     │  (若有ForEach则   │
│  _sort()        │     │   特殊处理)       │
└────────┬────────┘     └──────────────────┘
         │
         ▼
┌─────────────────┐
│  按序执行节点    │
│  for node in    │
│    sorted_nodes │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌────────┐ ┌────────┐
│ 普通节点│ │ForEach │
│ .forward│ │ 循环节点│
│ ()      │ │ (展开  │
│         │ │  迭代) │
└────┬───┘ └────┬───┘
     │          │
     ▼          ▼
┌─────────────────┐
│  收集输出       │
│  更新 Detail    │
│  缓存中间数据   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  通知 UI 更新   │
│  (Signal)       │
└─────────────────┘
```

### 5.2 TorchCompiler

将节点图编译为标准 `nn.Module`：

```python
class TorchCompiler:
    """
    节点图 → nn.Module 编译器
    
    流程:
    1. 遍历 /obj/ 路径下所有节点
    2. 收集每个节点的 nn.Module
    3. 根据连接关系构建 forward() 方法
    4. 生成 DynamicModule(nn.Module) 实例
    5. 支持导出为 .pt / .onnx / torchscript
    """
    
    def compile(self, graph: NodeGraph) -> nn.Module:
        """编译节点图为 nn.Module"""
        ...
    
    def export_pt(self, module: nn.Module, path: str):
        """导出 .pt 文件"""
        ...
    
    def export_onnx(self, module: nn.Module, path: str, dummy_input):
        """导出 ONNX 模型"""
        ...
    
    def export_torchscript(self, module: nn.Module, path: str):
        """导出 TorchScript"""
        ...
```

### 5.3 TrainingEngine

```python
class TrainingEngine(QThread):
    """
    训练引擎 —— 在独立线程中运行训练循环
    
    信号:
    - epoch_started(int)          # epoch 开始
    - epoch_finished(int, dict)   # epoch 结束，附带指标
    - batch_finished(int, dict)   # batch 结束
    - training_finished(dict)     # 训练完成
    - training_error(str)         # 训练错误
    - checkpoint_saved(str)       # 检查点已保存
    
    流程:
    1. 从 /train/ 路径读取配置
    2. 编译 /obj/ 路径的模型
    3. 初始化 DataLoader, Optimizer, Scheduler
    4. 执行 ForEach 循环 (epoch)
    5. 每个 epoch 内执行 ForEach 循环 (batch)
    6. 通过信号通知 UI 更新
    """
```

---

## 6. UI 架构

### 6.1 主窗口布局

```
┌──────────────────────────────────────────────────────────────┐
│ Menu Bar: File | Edit | Nodes | Training | View | Help       │
├──────────────────────────────────────────────────────────────┤
│ Tool Bar: [▶ Run] [⏹ Stop] | [Select] [Pan] | [Zoom Fit]   │
├──────────────┬───────────────────────────────────────────────┤
│              │                                               │
│   左侧面板    │           右侧 - 节点图编辑区                  │
│   (数据可视化) │                                               │
│              │  ┌─────────────────────────────────────────┐  │
│  ┌─────────┐ │  │  Path: /obj/nnmodel/                    │  │
│  │权重分布  │ │  ├─────────────────────────────────────────┤  │
│  │直方图    │ │  │                                         │  │
│  │         │ │  │    [Conv2d]                              │  │
│  │         │ │  │       │                                  │  │
│  └─────────┘ │  │    [BatchNorm]                           │  │
│              │  │       │                                  │  │
│  ┌─────────┐ │  │    [ReLU]          ┌──────────────┐     │  │
│  │特征图    │ │  │       │           │ 参数面板      │     │  │
│  │可视化    │ │  │    [MaxPool]      │ (右上角浮动)  │     │  │
│  │         │ │  │       │           │              │     │  │
│  │         │ │  │    [Linear]       │ in_features  │     │  │
│  └─────────┘ │  │       │           │ out_features │     │  │
│              │  │    [Softmax]      │ bias         │     │  │
│  ┌─────────┐ │  │                    │ expression   │     │  │
│  │层结构    │ │  │                    └──────────────┘     │  │
│  │链接关系  │ │  │                                         │  │
│  │         │ │  └─────────────────────────────────────────┘  │
│  └─────────┘ │                                               │
│              │                                               │
├──────────────┴───────────────────────────────────────────────┤
│ Status Bar: Ready | Current Path: /obj/nnmodel | GPU: CUDA   │
└──────────────────────────────────────────────────────────────┘
```

### 6.2 节点图形项结构

```
每个节点 (NodeGraphicsItem) 的视觉结构:

    ◄───── 节点宽度 (默认 180px) ─────►
    
    ┌───────── 输入引脚区 ─────────┐    ▲
    │  ●input_1    ●input_2        │    │
    ├──────────────────────────────┤    │
    │[⊘]│     [Conv2d]      │[👁]│    │ 节点高度
    │禁用│   图标+名称+选中   │显示│    │ (默认 52px)
    │ 1  │      区域 7       │ 1  │    │ + 引脚行高
    ├──────────────────────────────┤    │
    │  ●output_1   ●output_2      │    │
    └──────────────────────────────┘    ▼

    节点左侧: 禁用按钮 (占比 1/9)
    节点中间: 图标 + 名称 + 操控区域 (占比 7/9)
    节点右侧: 左侧视图显示按钮 (占比 1/9)
    
    引脚: 圆形小点，悬停显示 shape 信息
    节点: 悬停显示层结构概要
    
    连接方向: 从上到下 (输入在上方，输出在下方)
```

### 6.3 交互状态机

```
                    ┌─────────┐
         ┌─────────│  Idle    │◄────────────┐
         │         └────┬────┘             │
         │              │                   │
    双击节点        单击空白/节点         按Esc
         │              │                   │
         ▼              ▼                   │
    ┌─────────┐   ┌──────────┐             │
    │ Enter   │   │ Selecting│─── 拖拽 ──►│Dragging  │
    │ Subnet  │   │          │             │ Node     │
    └─────────┘   └──────┬───┘             └──────────┘
                         │
                    拖拽引脚
                         │
                         ▼
                   ┌──────────┐      释放在引脚上
                   │ Drawing  │────────────────►[创建Connection]
                   │ Connection│
                   └──────────┘      释放在空白处
                         │───────────────────►[取消]
```

---

## 7. 信号系统架构

### 7.1 GlobalSignalBus

```python
class GlobalSignalBus(QObject):
    """
    全局信号总线 —— 解耦各模块之间的通信
    
    所有跨模块的事件都通过此总线传递。
    每个模块只需要知道总线的存在，不需要直接引用其他模块。
    """
    
    # === 节点事件 ===
    node_created = Signal(str)          # node_id
    node_deleted = Signal(str)          # node_id
    node_moved = Signal(str, float, float)  # node_id, x, y
    node_selected = Signal(str)         # node_id (单选)
    node_deselected = Signal()
    node_renamed = Signal(str, str)     # node_id, new_name
    node_enabled_changed = Signal(str, bool)  # node_id, enabled
    node_visible_changed = Signal(str, bool)  # node_id, visible
    
    # === 连接事件 ===
    connection_created = Signal(str)    # connection_id
    connection_deleted = Signal(str)    # connection_id
    connection_enabled_changed = Signal(str, bool)
    
    # === 属性事件 ===
    property_changed = Signal(str, str, object)  # node_id, prop_name, value
    expression_changed = Signal(str, str, str)   # node_id, prop_name, expr
    
    # === 路径事件 ===
    path_changed = Signal(str)          # 当前浏览路径
    
    # === 图事件 ===
    graph_modified = Signal()           # 图发生了任何修改
    graph_executed = Signal(dict)       # 执行完成，附带结果
    
    # === 训练事件 ===
    training_started = Signal()
    training_stopped = Signal()
    training_epoch = Signal(int, dict)  # epoch, metrics
    training_batch = Signal(int, dict)  # batch, metrics
    
    # === 项目事件 ===
    project_new = Signal()
    project_opened = Signal(str)        # file_path
    project_saved = Signal(str)         # file_path
```

### 7.2 信号流示例

```
用户拖拽创建 Conv2d 节点:

UI: NodeGraphView.mousePressEvent()
 │
 ├──► UndoStack.push(CreateNodeCommand)
 │        │
 │        ├──► NodeGraph.add_node(conv2d_node)
 │        │
 │        └──► GlobalSignalBus.node_created.emit(node_id)
 │                    │
 │                    ├──► NodeGraphScene.on_node_created()   → 创建图形项
 │                    ├──► DataViewerPanel.on_node_created()  → 更新视图
 │                    ├──► NodeLibraryPanel.on_node_created() → 更新列表
 │                    └──► PropertiesPanel.on_node_created()  → 准备参数
 │
 └──► GlobalSignalBus.graph_modified.emit()
              │
              └──► MainWindow.on_graph_modified()  → 标记未保存
```

---

## 8. 撤销/重做系统

### 8.1 命令模式

```python
class Command(ABC):
    """命令基类"""
    
    @abstractmethod
    def execute(self) -> None: ...
    
    @abstractmethod
    def undo(self) -> None: ...
    
    def redo(self) -> None:
        self.execute()
    
    @property
    @abstractmethod
    def description(self) -> str: ...


class UndoStack:
    """撤销栈"""
    
    def __init__(self, max_size: int = 100):
        self._undo_stack: List[Command] = []
        self._redo_stack: List[Command] = []
        self._max_size = max_size
    
    def push(self, command: Command):
        """执行命令并入栈"""
        command.execute()
        self._undo_stack.append(command)
        self._redo_stack.clear()
        if len(self._undo_stack) > self._max_size:
            self._undo_stack.pop(0)
    
    def undo(self) -> bool: ...
    def redo(self) -> bool: ...
    def can_undo(self) -> bool: ...
    def can_redo(self) -> bool: ...
    def clear(self): ...
```

### 8.2 命令清单

| 命令类 | 触发操作 | execute() | undo() |
|--------|---------|-----------|--------|
| `CreateNodeCommand` | 创建节点 | add_node | remove_node |
| `DeleteNodeCommand` | 删除节点 | remove_node (+保存连接) | restore_node (+恢复连接) |
| `MoveNodeCommand` | 移动节点 | set_position(new) | set_position(old) |
| `RenameNodeCommand` | 重命名 | set_name(new) | set_name(old) |
| `CreateConnectionCommand` | 创建连接 | connect | disconnect |
| `DeleteConnectionCommand` | 删除连接 | disconnect | reconnect |
| `ChangePropertyCommand` | 修改属性 | set_prop(new) | set_prop(old) |
| `PasteNodesCommand` | 粘贴节点 | add_nodes | remove_nodes |
| `GroupToSubnetCommand` | 打包子网 | create_subnet | expand_subnet |
| `ToggleNodeEnabledCommand` | 启用/禁用 | set_enabled(new) | set_enabled(old) |

---

## 9. 线程模型

```
┌─────────────────────────────────────┐
│           Main Thread (UI)           │
│  - 所有 UI 操作                      │
│  - 节点图编辑                        │
│  - 信号/槽处理                       │
│  - 轻量级图执行 (推理)               │
└─────────────────┬───────────────────┘
                  │ Signal/Slot
                  │
┌─────────────────▼───────────────────┐
│       Training Thread               │
│  - 训练循环                          │
│  - 前向/反向传播                     │
│  - 梯度更新                          │
│  - 通过信号报告进度                  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│     Data Loading Thread (可选)       │
│  - DataLoader 的 num_workers        │
│  - 数据预处理和增强                  │
└─────────────────────────────────────┘
```

**线程安全规则:**

1. **UI 更新只在主线程**: 所有 `QWidget` 操作必须在主线程
2. **训练线程不触碰 UI**: 仅通过 Qt Signal 传递数据
3. **共享数据加锁**: `NodeGraph` 的读写用 `QMutex` 保护
4. **训练可中断**: 通过 `QThread.requestInterruption()` 安全停止

---

## 10. 错误处理策略

### 10.1 异常分级

| 级别 | 示例 | 处理方式 |
|------|------|---------|
| **FATAL** | PyQt 初始化失败 | 弹窗 + 退出 |
| **ERROR** | 节点执行异常 | 节点标红 + 状态栏 + 日志 |
| **WARNING** | 类型不匹配 | 节点黄色边框 + 日志 |
| **INFO** | 执行完成 | 状态栏 + 日志 |
| **DEBUG** | 中间数据 | 仅日志 |

### 10.2 节点错误可视化

```
正常节点:     ┌─────────────┐
              │   Conv2d    │  (灰色/蓝色边框)
              └─────────────┘

警告节点:     ┌─────────────┐
              │   Conv2d    │  (黄色边框 + ⚠ 图标)
              └─────────────┘

错误节点:     ┌─────────────┐
              │   Conv2d    │  (红色边框 + ✖ 图标)
              │  [Error Msg]│  (错误信息 tooltip)
              └─────────────┘

禁用节点:     ┌─────────────┐
              │   Conv2d    │  (半透明 + 虚线边框)
              └─────────────┘
```

---

## 11. 依赖关系图

```
main.py
  ├── config/settings.py
  ├── plugins/plugin_manager.py
  │     └── plugins/plugin_base.py
  ├── ui/main_window.py
  │     ├── ui/graphics/node_graphics_scene.py
  │     │     └── core/base/node_graph.py
  │     ├── ui/graphics/node_graphics_view.py
  │     │     └── ui/graphics/node_graphics_scene.py
  │     ├── ui/panels/data_viewer_panel.py
  │     │     └── ui/widgets/tensor_viewer.py
  │     │     └── ui/widgets/histogram_widget.py
  │     ├── ui/panels/properties_panel.py
  │     │     └── ui/widgets/parameter_widgets.py
  │     │     └── ui/widgets/expression_editor.py
  │     └── ui/themes/theme_manager.py
  └── core/
        ├── base/node.py
        │     └── base/pin.py
        ├── base/node_graph.py
        │     └── base/connection.py
        ├── base/node_registry.py
        │     └── nodes/**/*
        ├── engine/graph_executor.py
        │     └── base/node_graph.py
        ├── engine/torch_compiler.py
        │     └── base/node_graph.py
        ├── expressions/evaluator.py
        └── serialization/project_manager.py
```

---

*文档版本: v1.0*  
*最后更新: 2026-02-11*
